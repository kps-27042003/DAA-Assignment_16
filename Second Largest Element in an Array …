import java.util.Arrays;
public class Solution {
    public static int[] getSecondOrderElements(int n, int []a) {
        Arrays.sort(a);
        int arr[]=new int[2];
        int i=0;
        arr[i]=a[n-2];
        i++;
        arr[i]=a[1];
        return arr;
    }
}
